get:
  tags:
    - Shipments
  summary: Buscar entrega por ID
  parameters:
    - $ref: "../swagger.yaml#/components/parameters/IdParam"
  responses:
    200:
      description: Entrega encontrada
      content:
        application/json:
          schema:
            $ref: "../schemas/Shipment.yaml"
    404:
      description: Entrega não encontrada
